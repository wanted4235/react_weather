import React from 'react'
import s from '../ThisDayInfo/ThisDayInfo.module.scss'

type Props = {}

export const ThisDayInfo = (props: Props) => {
    return (
        <div className={s.thisDayInfo}>ThisDayInfo</div>
    )
}